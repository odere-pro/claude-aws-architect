# Changelog

All notable changes to `claude-aws-architect` are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

One entry is added per merged PR. The plugin starts at `v0.1.0` — no `v0.0.x` versions exist. Until v0.1.0 is tagged (per [PR-PLAN.md](./docs/plan/PR-PLAN.md) PR 31, which gates the tag on the release-dogfood gate), all PR entries accumulate under `## [Unreleased]` and are promoted to `## [0.1.0]` at tag time.

## [Unreleased]

### Added

- **PR 24**: Powers. Three declarative bundles under `powers/<name>.power.json` matching the canonical power-bundle schema (`name`, `version` SemVer, `description`, `mcpServers[]`, `skills[]`, `hooks[]`, `commands[]`; every name resolves to an existing entry in `.mcp.json`, `skills/`, `hooks/hooks.json`, or `commands/`). `powers/claude-aws-architect-cdk.power.json` (CDK authoring: `iac` + `cost` + `kb` MCP servers; routing/grounding/cache plus `aws-component-contract`, `aws-layered-diagram`, and the cost/reliability/operational-excellence pillar skills; both default hooks; all three commands). `powers/claude-aws-architect-cost.power.json` (cost-only workflow: `cost` + `kb` MCP servers; routing/grounding/cache plus `aws-waf-cost-optimization-skill`; `secret-scanner` hook; `/aws` and `/aws-spec` commands). `powers/claude-aws-architect-security.power.json` (security-only workflow: `sec` + `iam` + `kb` MCP servers; routing/grounding/cache plus `aws-waf-security-skill`; both default hooks; all three commands). README's `## Powers` section drops the "Land in PR 24" placeholder. Gate 6 (powers) transitions from no-op PASS to enforcing PASS — three power files match the power-bundle schema. Closes the v0.1.0 power set per F7; Bedrock and IaC-foundations powers deferred to v0.2.

- **PR 0**: Planning import. `docs/plan/SPEC-v4.md` (full v4 declarative spec, sections 0–18), `docs/plan/PR-PLAN.md` (33-PR execution roadmap with dependencies and parallel-safe flags), `docs/plan/PR-CONVENTIONS.md` (branch naming, commit format, merge strategy).
- **PR 1**: Plugin scaffold. `.claude-plugin/plugin.json` (manifest with `name`, `version`, `engines.claude-code`), `.mcp.json` (6 AWS MCP servers with short identifiers, pinned versions, and `timeoutMs`), `README.md` (with Trademark notice), `SPEC.md`, `LICENSE` (MIT), `SECURITY.md` (vulnerability disclosure), `SUPPORT.md` (community-driven posture), `CHANGELOG.md` (this file).
- **PR 2**: Deterministic gates 1–18 in CI. 16 gate scripts under `tests/gates/` (gates 1 and 13 deferred until PR 27 ships init/doctor); `tests/gates/lib/common.sh` shared helpers (bash 3.2 compatible — uses `read_into` instead of `mapfile`); cached MCP tool lists under `tests/gates/cache/tools-<key>.txt` (seeded from upstream awslabs READMEs); `tests/gates/cache/known-overshoots.txt` documenting 12 upstream-controlled tool names that exceed the 64-char tool-name budget (WARN-only, with citation of which agent/skill depends on each); `.markdownlint.jsonc` config with rationale for every disabled rule; `.github/workflows/gates.yml` with three jobs (Linux bash 5+, macOS system bash 3.2, macOS brew bash 5+) — explicit `/bin/bash` invocation on macOS asserts the "tested on macOS bash 3.2" claim. Markdown auto-formatted with prettier 3.6.2.
- **PR 3**: Transcript-replay harness skeleton. `tests/run-transcripts.sh` (validate-only mode at v0.1.0; execute and update-snapshots modes stubbed pending PR 18); `tests/transcripts/README.md` (fixture format reference); first fixture `tests/transcripts/vibe-shallow/` (validates the verb-of-inquiry → shallow depth rule — no fan-out, no artefacts written) with `prompt.txt`, `expected-tools.jsonl`, and `expected-artefacts.txt`. New CI job `transcript-validate` wires the harness into `.github/workflows/gates.yml`. Makes gate 19 (skeleton) achievable.
- **PR 4**: Workflow skill `aws-mcp-routing`. `skills/aws-mcp-routing/SKILL.md` (canonical workflow-skill form: `**WORKFLOW SKILL** —` description, version, all five mandatory body sections including Gotchas); sibling `trigger-keywords.txt` (24 keywords, gate-16 input); three references — `server-roster.md` (the 6-server table + per-server tool catalogue), `selection-rules.md` (intent-to-server mapping + tool-selection priorities + pre/post-call obligations), `degraded-modes.md` (per-server fallback markers + lifecycle through merge contract + retry policy: none at v0.1.0). First skill ever; gates 5/10/16/17 transition from no-op PASS to enforcing PASS.
- **PR 5**: Workflow skill `aws-spec-grounding`. `skills/aws-spec-grounding/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (25 keywords); three references — `cite-format.md` (`<server>:<short-key>` citation format, frontmatter vs inline placement, validation rules), `grounded-by-rules.md` (per-artefact minimums for requirements/design/tasks/contracts/ADRs, what counts as a rationale, detection algorithm, surfacing violations), `opinion-vs-fact.md` (boundary tests, examples, edge cases, resolution algorithm). Enforces that every factual AWS claim carries ≥1 citation and every design opinion carries a rationale.
- **PR 6**: Workflow skill `aws-grounding-cache`. `skills/aws-grounding-cache/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (27 keywords); four references — `ledger-schema.md` (top-level structure, entry schema with `server`/`tool`/`query`/`retrieved`/`result_summary`/`ttl-class`/`short_key_ext`, conflict-resolution log, validation rules), `ttl-policy.md` (per-class windows: pricing/quota 30d, api-shape/region 90d, immutable indefinite; read-side behaviour, degraded-MCP interaction, write-side timestamp discipline), `collision-policy.md` (canonicalisation algorithm, SHA-1 short-key derivation with real verifiable example values, 8→12-hex extension on collision, lookup fallback algorithm with mandatory query-equality check at every step), `redaction-policy.md` (8-pattern catalogue with application order, negative cases, write-abort-on-failure semantics); plus `assets/grounding-ledger.json.tmpl` starter file. Enables the `<server>:<short-key>` citations consumed by `aws-spec-grounding`.

- **PR 7**: Workflow skill `aws-component-contract`. `skills/aws-component-contract/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (30 keywords); two references — `contract-schema.md` (file-path rule, frontmatter keys, closed `kind` vocabulary of 21 AWS resource kinds, seven canonical body sections, status-transition table, validation rules summary), `observability-triple.md` (why three signals, what counts as a concrete metric/log/trace, what does NOT count, why lint refuses to auto-fill, interaction with the `cw` MCP server); plus `assets/contract.md.tmpl` starter file with all required frontmatter keys and seven body sections in canonical order.

- **PR 8**: Workflow skill `aws-layered-diagram`. `skills/aws-layered-diagram/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (26 keywords); two references — `layer-taxonomy.md` (six required tags `c4-l1`/`c4-l2`/`c4-l3-<container>`/`seq-system`/`seq-component`/`seq-error`, tag-name rules including bare-`c4-l3` rejection, per-layer content rules, top-level node rule, orphan-node rule, cross-layer consistency boundary), `c4-and-sequence.md` (C4 model recap with audience/granularity/intent for each level, what belongs and what does NOT in L1/L2/L3, sequence-layer participants/story/length guidance, notation conventions); plus `assets/diagrams.d2.tmpl` starter file with all six required layer blocks pre-populated using d2 default syntax.

- **PR 9**: Workflow skill `aws-sdlc-workflow`. `skills/aws-sdlc-workflow/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (29 keywords); three references — `phases.md` (five SDLC phases vibe/discovery/design/plan/validate with triggers, artefacts, and specialists; depth-escalation heuristic with 5 ordered rules; mid-turn re-evaluation policy; per-phase responsibilities), `parallel-fanout.md` (single-message multi-`Agent`-call rule, specialist eligibility per phase, MCP-call budgets 8/12/16 per discovery/solution-architect/implementation, specialist-to-server map, when sequencing is legitimate, result handling), `merge-rules.md` (5-rule conflict-resolution priority order security→facts→cost→convergence→recency, what counts as a conflict, conflict-surfacing format under `## Open Questions`, logging discipline against the grounding ledger). Closes out the workflow-skill set: 6 of 6 workflow skills now exist.
- **PR 15**: Pillar skill `aws-waf-sustainability-skill`. `skills/aws-waf-sustainability-skill/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (40 keywords); two references — `region-carbon-tiers.md` (carbon-tier source-of-truth policy citing the published per-region carbon-intensity table, four-tier table T1/T2/T3/U with deviation policy enumerating the four legitimate constraints — data residency, latency floor, service availability, DR pairing — and the `region_tier_distribution` aggregate block the orchestrator emits), `graviton-eligibility.md` (Graviton arm64 default, six legitimate `requires-x86` rationales with verification approach, six rejected rationales the agent must rewrite, and the YAML `sustainability:` recording format including the mandatory `<server>:<short-key>` citation when an exception applies). First pillar skill; covers region-selection, Graviton right-sizing, managed/serverless defaults, storage lifecycle, idle handling, and data-transfer minimisation.

- **PR 10**: WAF pillar skill `aws-waf-operational-excellence-skill`. `skills/aws-waf-operational-excellence-skill/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (32 keywords); three references — `oe-design-questions.md` (24 OE design questions across organisation/observability/SLOs/runbooks/deployment/telemetry; cross-pillar boundary), `oe-antipatterns.md` (22 anti-patterns AP-OE-1..22 with severity HIGH/MEDIUM/LOW guidance, evidence requirements, what is NOT an anti-pattern), `oe-checklist.md` (29 checklist items with PASS/DEFER/GAP semantics, severity rollup for the review block, transition-blocking rules); plus `assets/oe-review.md.tmpl` starter for the per-feature OE review block in `design.md`. First of six WAF pillar skills.

- **PR 11**: WAF pillar skill `aws-waf-security-skill`. `skills/aws-waf-security-skill/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (37 keywords); three references — `sec-design-questions.md` (24 Security design questions Q-SEC-1..24 across identity-and-access, data protection, identity perimeter and network exposure, detection and response, threat modelling; cross-pillar boundary), `sec-antipatterns.md` (24 anti-patterns AP-SEC-1..24 covering wildcard IAM actions, missing encryption decisions, public-by-default stateful resources, missing threat model, with severity guidance and what is NOT an anti-pattern), `sec-checklist.md` (27 checklist items SECC-1..27 with PASS/DEFER/GAP semantics, severity rollup, transition-blocking rules); plus `assets/sec-review.md.tmpl` starter for the per-feature Security review block in `design.md`. Second of six WAF pillar skills; structurally inherits from `aws-waf-operational-excellence-skill` per the locked WAF-pillar conventions.

- **PR 14**: Pillar skill `aws-waf-cost-optimization-skill`. `skills/aws-waf-cost-optimization-skill/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (51 keywords); two references — `pricing-models.md` (5-step decision tree on-demand/reserved/savings-plan/spot/lambda-fargate; workload-class eligibility table for 10 classes with explicit Spot-ineligible flags; commitment-term, coverage, and payment-option decision policy; five anti-patterns the agent rewrites), `storage-tiers.md` (default S3 lifecycle ladder with minimum-storage-duration trap table for all five S3 tiers; Intelligent-Tiering eligibility; EBS gp3 default; CloudWatch Logs retention defaults by use case; aggregate `storage_summary` block; four anti-patterns). Pillar skill enforcing `cost` MCP citation for every priced resource, mandatory pricing-model declaration on long-running resources, four mandatory cost-allocation tags, and at least one Budgets alarm threshold per design.
- **PR 28a**: Security and supply-chain infrastructure. [`docs/threat-model.md`](./docs/threat-model.md) (per SPEC-v4 §16.3 — three trust boundaries; four threat classes T1 prompt-injection-via-MCP / T2 hook-script-abuse / T3 install-time-tampering / T4 secret-exfiltration-via-grounding-ledger, each with attack vector, impact, mitigation, detection, residual risk; out-of-model risks; update cadence tied to `.mcp.json`, `hooks/scripts/`, `scripts/`, and the grounding-ledger schema). [`.github/dependabot.yml`](./.github/dependabot.yml) (GitHub Actions ecosystem only at v0.1.0 — weekly Monday 06:00 UTC, grouped minor/patch updates, `chore(deps)` commit prefix; rationale comment explaining why no other ecosystem is monitored). [`.github/workflows/mcp-version-skew.yml`](./.github/workflows/mcp-version-skew.yml) (nightly cron `17 4 * * *` plus `workflow_dispatch`; jq-driven scan of every `command: uvx` server in `.mcp.json` against the PyPI JSON API; pre-release shapes `aN`/`bN`/`rcN`/`.devN` filtered out; idempotent issue lifecycle — opens or updates a single `MCP version skew` issue when drift is detected, closes it with confirmation comment when drift clears; `actions/checkout` SHA-pinned). `SECURITY.md` updated to remove "lands in PR 28a" deferrals and link the threat model, dependabot config, and skew workflow inline.
- **PR 28b**: Issue and PR templates. `.github/ISSUE_TEMPLATE/config.yml` (blank issues disabled; routes questions to Discussions, vulnerabilities to private security advisories, support-policy questions to `SUPPORT.md`), `.github/ISSUE_TEMPLATE/bug_report.yml` (preflight checks, reproduction, plugin/Claude Code/OS metadata, MCP-server snapshot for version-skew triage, suggested-severity dropdown matching the `SUPPORT.md` rubric), `.github/ISSUE_TEMPLATE/feature_request.yml` (problem-first framing, surface-area multi-select, target-version triage), `.github/pull_request_template.md` (mirrors the body template authored in `docs/plan/PR-CONVENTIONS.md`: What/Why/Gates exercised/Depends on/Test plan/Out of scope plus a merge-strategy selector and pre-merge checklist). `SUPPORT.md` updated to link the new templates directly via `?template=` query strings.
- **PR 21**: L3 implementation agent (bundled v0.1.0 specialist). `agents/claude-aws-architect-implementation-agent.md` (canonical L3 agent declaration: frontmatter with `name`, two-half `description` (USE FOR / DO NOT USE FOR), `model`, `effort: high`, `user-invocable: false`, `tools` without the `Agent` tool, `color: orange`; nine H2 sections in canonical order — Role, Requirements, Dependencies, Operating Principles, Routing Map, Workflow, Output Rules, Boundaries, Quality Checks). The bundled v0.1.0 specialist combines IaC drafting, cost ROM estimation, IAM hygiene, and test-sketch authoring under one merge contract; splits into `cost-engineer`, `security-engineer`, and `test-engineer` specialists in v0.2 once the merge contract is validated under load. Dependencies expanded to nine sub-tables: MCP servers (`iac`, `cost`, `iam`, `cw` — never `kb` or `sec`), Skills (`aws-component-contract`, `aws-mcp-routing`, plus four `aws-waf-*-skill` pillars: security, cost-optimization, reliability, operational-excellence), Rules (`aws-cdk`, `aws-iam-policy`, `aws-component-contract`, `aws-test`), Sibling agents (none), Output paths (`contracts/<slug>.md` section-merge writes covering IaC/Cost/Security/Acceptance/Observability quintet + `tasks.md` whole-file write at status `draft`), Reads, Writes, Calls (none), MCP tool budget (12 calls per invocation per O5 with `budget-exhausted` marker on overrun). Operating Principles enforce: design is read-only input (never re-architected); per-pillar `aws-waf-*-skill` gate before each section closes; cache reads precede MCP reads; status never advances past `draft`; single-source MCP discipline per section (IaC→`iac`, Cost→`cost`, Security→`iam`, Observability→`cw`); no live AWS API writes (read-class MCP tools only; write-class blocked by `aws-api-write-guard` hook); acceptance traces to requirements; wildcard IAM rejected outside the read-only carveout (`iam-advisory-only` marker if unavoidable). Six degraded markers defined: `cost-rom-only`, `iam-advisory-only`, `observability-incomplete`, `validation-advisory`, `budget-exhausted`, `traceability-missing`. Gate 9 PASS (4 agents); gate 5 PASS (28 frontmatter blocks); gate 14 markdown + prettier clean. Closes the v0.1.0 L3 specialist roster (orchestrator + 3 specialists). Fan-out wiring (N=3) to all three specialists lands in a follow-up integration step.

- **PR 18**: Orchestrator → discovery wiring + deterministic transcript executor. `agents/claude-aws-architect-orchestrator-agent.md` updated: Role paragraph drops the "scaffold only" framing; the Discovery delegation row in the Sibling agents table and the matching Routing Map row both flip to **active**; Workflow steps 3–5 rewrite the full-depth path so a single `Agent` call to `claude-aws-architect-discovery-agent` issues when the active phase is Discovery (no `requirements.md` exists yet), Design and Plan phases continue to emit a staged notice pending solution-architect / implementation wiring; merge step describes pass-through behaviour for single-specialist returns; final-assembly step writes `requirements.md` on Discovery turns. `tests/run-transcripts.sh` gains an `--execute` mode that replays the §5.6 depth-classification heuristic + routing decisions deterministically (no live model): shallow prompts assert no-fan-out and zero artefact writes; full-depth prompts whose first-step trace expects a single `discovery-agent` Agent call resolve to PASS, and prompts expecting parallel fan-out to multiple specialists resolve to DEFERRED (not FAIL) until the solution-architect / implementation wiring lands. Header docstring rewritten to match the new mode contract; `--update-snapshots` fails fast with a clear v0.1.0 not-implemented message. `vibe-shallow` now passes end-to-end under `--execute`; `sdlc-full-depth` correctly defers. Gate 4 (shellcheck) clean; gate 9 (agents) PASS for all 3 agents; gate 14 (markdown + prettier) clean.
- **PR 17**: L3 discovery agent + transcript fixture. `agents/claude-aws-architect-discovery-agent.md` (canonical L3 agent declaration: frontmatter with `name`, two-half `description` (USE FOR / DO NOT USE FOR), `model`, `effort: medium`, `user-invocable: false`, `tools` without the `Agent` tool, `color`; nine H2 sections in canonical order — Role, Requirements, Dependencies, Operating Principles, Routing Map, Workflow, Output Rules, Boundaries, Quality Checks; Dependencies expanded to nine sub-tables — MCP servers (`kb` only), Skills (`aws-spec-grounding`, `aws-grounding-cache`, `aws-mcp-routing`), Rules (`aws-spec-frontmatter`, `aws-docs`), Sibling agents (none), Output paths (`requirements.md` draft + `.grounding-ledger.json`), Reads, Writes, Calls (none), MCP tool budget (8 calls per O5)). `tests/transcripts/sdlc-full-depth/` fixture (gate 19 schema-valid; execution wires up in PR 18) with `prompt.txt` (event-driven order-processing pipeline prompt that triggers SDLC-artefact intent → full depth per §5.6 rule 2), `expected-tools.jsonl` (3 parallel `Agent` calls at step 1 covering all three L3 specialists, then per-specialist MCP traffic across `kb`, `iac`, `cost`, `iam`, `cw`, then 4 final-assembly Writes), `expected-artefacts.txt` (every F5+F6 path: `requirements.md`, `design.md`, `tasks.md`, `diagrams.d2`, four `contracts/<slug>.md`, `.grounding-ledger.json`), `expected-merge.json` (lists three specialists; empty `open_questions` and `priority_rules_applied` baseline). Agent passes gate 9; fixture passes gate 19. Wiring of full-depth fan-out to this agent lands in PR 18.
- **PR 16**: Agent `claude-aws-architect-orchestrator-agent`. `agents/claude-aws-architect-orchestrator-agent.md` (canonical L4 agent declaration: full frontmatter including `model`, `effort`, `user-invocable`, `tools` with `Agent`, `max-iterations: 3`, `argument-hint`, `color`; nine H2 sections in canonical order — Role, Requirements, Dependencies (5 sub-tables: MCP servers, Skills, Rules, Sibling agents, Output paths), Operating Principles, Routing Map, Workflow, Output Rules, Boundaries, Quality Checks). Scaffold-only at v0.1.0: the shallow direct-answer path is active; full-depth fan-out to discovery/solution-architect/implementation specialists ships its declarative contract but the workflow returns a staged-notice for full-depth turns until the wiring lands in a follow-up integration step. Gate 9 (agents) transitions from no-op PASS to enforcing PASS.
- **PR 28a**: Security and supply-chain infrastructure. [`docs/threat-model.md`](./docs/threat-model.md) (per SPEC-v4 §16.3 — three trust boundaries; four threat classes T1 prompt-injection-via-MCP / T2 hook-script-abuse / T3 install-time-tampering / T4 secret-exfiltration-via-grounding-ledger, each with attack vector, impact, mitigation, detection, residual risk; out-of-model risks; update cadence tied to `.mcp.json`, `hooks/scripts/`, `scripts/`, and the grounding-ledger schema). [`.github/dependabot.yml`](./.github/dependabot.yml) (GitHub Actions ecosystem only at v0.1.0 — weekly Monday 06:00 UTC, grouped minor/patch updates, `chore(deps)` commit prefix; rationale comment explaining why no other ecosystem is monitored). [`.github/workflows/mcp-version-skew.yml`](./.github/workflows/mcp-version-skew.yml) (nightly cron `17 4 * * *` plus `workflow_dispatch`; jq-driven scan of every `command: uvx` server in `.mcp.json` against the PyPI JSON API; pre-release shapes `aN`/`bN`/`rcN`/`.devN` filtered out; idempotent issue lifecycle — opens or updates a single `MCP version skew` issue when drift is detected, closes it with confirmation comment when drift clears; `actions/checkout` SHA-pinned). `SECURITY.md` updated to remove "lands in PR 28a" deferrals and link the threat model, dependabot config, and skew workflow inline.

- **PR 26**: Secondary hooks. Four PostToolUse advisory hooks under `hooks/scripts/` plus matching registry entries in `hooks/hooks.json`, all default-disabled per SPEC-v4 §7.2 rows 4–7. `on-cdk-write.sh` (Write/Edit/MultiEdit/NotebookEdit on `bin/`, `lib/`, `cdk/`, `infrastructure/`, `iac/` source roots and `cdk.json` / `cdk.context.json`; surfaces a `cdk synth` + `cdk diff` + cdk-nag nudge on stderr; never runs synth automatically). `on-iam-write.sh` (Write/Edit/MultiEdit on IAM-shaped JSON paths under `iam/` or `policies/` or basenames matching `*policy*.json` / `*role*.json` / `iam-*.json`; jq-based JSON parse-test plus heuristic findings for `Action: "*"` wildcards, service-wildcard like `s3:*`, `Resource: "*"` wildcards, missing Condition block when paired with a wildcard, and trust-policy `Principal.AWS: "*"` without source-account/source-arn condition; deeper review delegated to `iam:simulate_principal_policy` via the implementation agent — hook itself never makes MCP calls). `on-bedrock-prompt-write.sh` (Write/Edit/MultiEdit on prompt artefacts under `prompts/`, `bedrock-prompts/`, `agentcore/` directories or basenames starting with `prompt-`/`bedrock-` with `.json|.yaml|.yml|.md`; checks the three v0.1.0 mandatory bindings — model-id pinning, guardrail binding, evaluation-hook presence — surfacing each missing field as a separate stderr line). `aws-test-coverage.sh` (Write/Edit/MultiEdit on `.ts|.tsx|.js|.jsx|.py` files that import an AWS SDK or CDK; computes six candidate sibling-test paths per language and asserts at least one exists; flags stale tests via `-nt` mtime comparison when the implementation file is newer than its test; never runs the test suite). All four scripts source `hooks/scripts/lib/dispatcher.sh`, emit JSON `permissionDecision: "allow"` (PostToolUse advisory; the write has already landed by the time the hook fires), shellcheck-clean, bash 3.2 compatible, no MCP traffic, no network calls. `hooks/hooks.json` adds a `PostToolUse` array with the four entries; each entry carries `enabledByDefault: false` and a `filePattern` regex matching the script-internal scope. `hooks/README.md` updated with the full v0.1.0 hook table (now 6 hooks: 2 PreToolUse default-on plus 4 PostToolUse default-off) and per-hook manual-test recipes. `tests/gates/gate-07-hooks-json.sh` switched from `// "__MISSING__"` (which jq treats `false` as null and would falsely flag `enabledByDefault: false` as missing) to `has(key)` — the latent bug only mattered once a default-disabled hook shipped. Gates 4 (shellcheck), 7 (hooks.json schema), 11 (hook-script contract) remain PASS.

- **PR 25**: Hooks core. `hooks/hooks.json` (registry; two PreToolUse entries; passes gate 7 schema with `name`, `event`, `matcher`, `command` referencing `${CLAUDE_PLUGIN_ROOT}`, `enabledByDefault`); `hooks/scripts/lib/dispatcher.sh` (shared bash 3.2-compatible helpers: `hook_read_input`, `hook_field` wrapping jq with safe defaults, `hook_log` structured stderr, `hook_emit_decision` / `hook_allow` / `hook_ask` / `hook_deny` JSON output emitting `permissionDecision` allow/ask/deny per Claude Code hook convention); `hooks/scripts/secret-scanner.sh` (PreToolUse on `Write|Edit|MultiEdit|NotebookEdit`; 5-pattern catalogue blocking AWS access keys (AKIA/ASIA/AROA/AIDA/AGPA/ANPA/ANVA/APKA/ASCA/AIPA followed by 16 alphanumerics), aws_secret_access_key assignments, aws_session_token, BEGIN-PRIVATE-KEY blocks for RSA/EC/DSA/OPENSSH/PGP/ENCRYPTED, KMS CMK ARNs; emits deny + stderr naming the matched label without echoing the value); `hooks/scripts/aws-api-write-guard.sh` (PreToolUse on `mcp__.*`; verb extraction from tool name suffix; 41-verb write classifier covering ; emits ask with the resolved server/tool/verb in the reason); `hooks/README.md` (hook registry table, decision contract, disable instructions, manual-test recipes). Both hooks shellcheck-clean and bash 3.2 compatible. Gates 7 and 11 transition from no-op PASS to enforcing PASS.

- **PR 12**: WAF pillar skill `aws-waf-reliability-skill`. `skills/aws-waf-reliability-skill/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (62 keywords); three references — `rel-design-questions.md` (24 Reliability design questions Q-REL-1..24 across recovery objectives and DR strategy, availability posture, runtime resilience, capacity and quotas, health/detection/recovery, dependency and failure-mode analysis; cross-pillar boundary), `rel-antipatterns.md` (25 anti-patterns AP-REL-1..25 covering RTO/RPO undefined, single-AZ stateful without rationale, naked timeout, unbounded retries, non-idempotent retried writes, quota assumption absent, FMA absent; severity guidance and what is NOT an anti-pattern), `rel-checklist.md` (28 checklist items RELC-1..28 with PASS/DEFER/GAP semantics, severity rollup, transition-blocking rules); plus `assets/rel-review.md.tmpl` starter for the per-feature Reliability review block in `design.md` with a recovery-objectives summary table and a failure-mode-analysis table. Inherits structurally from prior WAF pillar skills per the locked WAF-pillar conventions.

- **PR 22**: Rules roster. Nine file-scoped instruction files under `rules/<name>.instructions.md`, each with the canonical frontmatter (`description`, `applyTo` glob, `inclusion: always|conditional|manual`) and a 2–6-bullet ≤200-word body that states what must hold in the matched files: `aws-cdk.instructions.md` (CDK construct/stack/prop hygiene, removal-policy explicitness, no inline IAM JSON), `aws-iam-policy.instructions.md` (least-privilege, ARN scoping, condition keys, trust-policy discipline), `aws-sdk-usage.instructions.md` (client lifetime, retries, pagination, region resolution), `aws-bedrock-prompt.instructions.md` (prompt structure, model-id pinning, guardrail binding, response-shape contract), `aws-test.instructions.md` (mocking layer, LocalStack-vs-real-AWS, fixture isolation, deterministic seeds), `aws-docs.instructions.md` (audience-first headings, C4 cross-walk, citation coverage, no marketing language), `aws-diagram.instructions.md` (six required layer tags, node naming, orphan rule), `aws-component-contract.instructions.md` (frontmatter, seven canonical sections, observability triple, talks-to resolution), `aws-spec-frontmatter.instructions.md` (frontmatter keys, grounded-by minimums, status-transition discipline). Gate 8 (rules) transitions from no-op PASS to enforcing PASS: nine rule files match the rule contract.

- **PR 19**: Agent `claude-aws-architect-solution-architect-agent`. `agents/claude-aws-architect-solution-architect-agent.md` (canonical L3 agent declaration: full frontmatter including `model`, `effort`, `user-invocable: false`, `tools`, `color`; nine H2 sections in canonical order — Role, Requirements, Dependencies (5 sub-tables: MCP servers, Skills, Rules, Sibling agents, Output paths), Operating Principles, Routing Map, Workflow, Output Rules, Boundaries, Quality Checks). Declares `aws-knowledge` and `aws-iac` MCP servers, the per-pillar fan-out across all six `aws-waf-*-skill` pillars, the layered-diagram c4-l1/c4-l2 split with downstream specialists, the component-contract one-to-one mapping with `design.md`, and the 12-call MCP budget. Declares the parallel fan-out invocation contract (`fan_out_index`, sibling slugs in flight) and the merge-contract markers (`grounding-deferred`, `budget-exhausted`, `requirements-incomplete`) that surface back to the orchestrator's `## Open Questions` and `## Degraded signals` sections. Outputs `design.md`, one `contracts/<slug>.md` per component, and the `c4-l1`/`c4-l2` blocks of `diagrams.d2`. Gate 9 (agents) continues to PASS with 2 agent files now matching the agent contract.

- **PR 21**: Agent `claude-aws-architect-implementation-agent`. `agents/claude-aws-architect-implementation-agent.md` (canonical L3 agent declaration: full frontmatter including `model`, `effort: high`, `user-invocable: false`, `tools` without the `Agent` tool, `color`; nine H2 sections in canonical order — Role, Requirements, Dependencies (9 sub-tables: MCP servers, Skills, Rules, Sibling agents, Output paths, Reads, Writes, Calls, MCP tool budget), Operating Principles, Routing Map, Workflow, Output Rules, Boundaries, Quality Checks). Declares the four-server MCP surface `iac`/`cost`/`iam`/`cw`, the four pillar skills `aws-waf-security-skill`/`aws-waf-cost-optimization-skill`/`aws-waf-reliability-skill`/`aws-waf-operational-excellence-skill`, the four rules `aws-cdk`/`aws-iam-policy`/`aws-component-contract`/`aws-test`, the section-merge write semantics for `contracts/<slug>.md` (IaC/Cost/Security/Acceptance/Observability only — Purpose/Interface/Integration links remain solution-architect territory), the 12-call MCP budget, the per-pillar guardrail-before-section-close discipline, the requirements-traceability rule for `tasks.md`, the wildcard-IAM rejection-with-`iam-advisory-only` fallback, and the no-live-AWS-API-writes boundary backed by the `aws-api-write-guard` hook. Closes the v0.1.0 L3 specialist roster (discovery + solution-architect + implementation), enabling the orchestrator's N=3 fan-out cap from N13. Gate 9 continues to PASS with 4 agent files now matching the agent contract.

- **PR 13**: WAF pillar skill `aws-waf-performance-efficiency-skill`. `skills/aws-waf-performance-efficiency-skill/SKILL.md` (canonical workflow-skill form, all five mandatory body sections); sibling `trigger-keywords.txt` (39 keywords); three references — `pe-design-questions.md` (24 PE design questions Q-PE-1..24 across latency/throughput targets, compute selection, data layer, caching, network/transfer, load testing; cross-pillar boundary), `pe-antipatterns.md` (23 anti-patterns AP-PE-1..23 covering implicit latency target, cold-start exposure, unbounded concurrency, hot keys, missing cache, no load-test plan; severity guidance and what is NOT an anti-pattern), `pe-checklist.md` (28 checklist items PEC-1..28 with PASS/DEFER/GAP semantics, severity rollup, transition-blocking rules); plus `assets/pe-review.md.tmpl` starter for the per-feature PE review block in `design.md` with latency/throughput summary table and load-test plan section. Inherits structurally from prior WAF pillar skills per the locked WAF-pillar conventions.

[Unreleased]: https://github.com/odere-pro/claude-aws-architect/compare/main...HEAD
